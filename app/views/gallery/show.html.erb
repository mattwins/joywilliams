<h1><%= @category.title %></h1>

<% @images.each do |image| -%>
  <div class="gall_image">
    <h3><%= image.title %></h3>
    <p><%= image.description %></p>
    <%= link_to image_tag(image.photo.url(:thumb)), image.photo.url(:medium), {:rel => 'prettyPhoto'}  %>
  </div>
<% end -%>